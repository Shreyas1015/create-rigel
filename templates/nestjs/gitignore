node_modules/
dist/
.env
.env.local
coverage/
*.log
.DS_Store
.vscode/

# Deterministic-eval transient artifacts (PLAN-003). The proof — .rigel/redgreen/ and
# .rigel/ac-results/ — is committed; these are throwaway.
reports/
.rigel/acceptance.unlock
.rigel/blast/
.rigel/.jest-*.json
.rigel/mutation/

# Ephemeral session resume pointer (local only — see docs/design-docs/memory.md)
STATE.md
