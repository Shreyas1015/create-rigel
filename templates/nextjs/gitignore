# Dependencies
node_modules/
.npm

# Build output
.next/
out/
dist/
*.tsbuildinfo
# next-env.d.ts is auto-generated by Next on every build — never commit it (DF-14)
next-env.d.ts

# Environment — NEVER commit
.env
.env.local
.env.*.local

# Generated — regenerate with /api-sync
# DO NOT add api.generated.ts here — it should be committed
# so TypeScript errors are caught in CI without running /api-sync

# Testing
coverage/
playwright-report/
test-results/
.nyc_output

# OS
.DS_Store
Thumbs.db

# IDE
.idea/
.vscode/
*.swp

# Vercel
.vercel

# Deterministic-eval transient artifacts (PLAN-003 / PLAN-004). The PROOF
# (.rigel/redgreen/, .rigel/ac-results/) and the judge review queue
# (.rigel/judge-review-queue/) are committed; everything below is throwaway.
.rigel/acceptance.unlock
.rigel/.vitest-*.json
.rigel/.jest-*.json
.rigel/mutation/
.rigel/screens/
reports/mutation/

# Impeccable skills are installed by /infra-setup (npx impeccable install) — regenerable,
# not committed (like node_modules). The detector + Rigel's severity config in .claude/hooks/
# ARE committed. (DF-4)
.claude/skills/impeccable/
.github/skills/impeccable/
