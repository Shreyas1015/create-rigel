name: CI

on:
  pull_request:
  push:
    branches: [main]

# Least-privilege default token. Jobs opt into more only where needed.
permissions:
  contents: read

# Cancel superseded runs on the same ref.
concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  verify:
    name: typecheck · lint · format · test · build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      # Harness integrity (PLAN-008 AC-2). FIRST: it is the cheapest check, and a tampered
      # harness invalidates every check after it. Needs neither package.json nor `npm ci` —
      # just the runner's preinstalled node, .rigel/manifest.json, and the stamped
      # scripts/lib/rigel-manifest.mjs, so it runs even on a bare (pre-/infra-setup) scaffold.
      - name: Verify Rigel manifest
        run: node scripts/rigel-verify.mjs

      # The bare template has no package.json (it's generated by /infra-setup).
      # Skip the real steps until an app exists so this stays green on the harness itself.
      - name: Detect app
        id: app
        run: |
          if [ -f package.json ]; then
            echo "exists=true" >> "$GITHUB_OUTPUT"
          else
            echo "exists=false" >> "$GITHUB_OUTPUT"
            echo "::notice::No package.json yet (bare harness) — skipping verify steps. Run /infra-setup to scaffold the app."
          fi

      - uses: actions/setup-node@v4
        if: steps.app.outputs.exists == 'true'
        with:
          node-version: 'lts/*'
          cache: npm

      - name: Install
        if: steps.app.outputs.exists == 'true'
        run: npm ci

      # THE gate, invoked as one command rather than re-listed step by step. This file used to run
      # typecheck/lint/format/coverage only — verify:rigel, knowledge, debug:regression and
      # contract:freshness were never run in CI at all, so provenance, the memory loop and the
      # generated-types freshness check were enforced only on someone's laptop. Re-listing the
      # steps here is what let them drift; calling the gate makes drift impossible.
      # Guarded by scripts/check-ci-mirrors-gate.mjs.
      - name: Gate (verify:rigel · typecheck · lint · format · coverage · knowledge · regressions · contract)
        if: steps.app.outputs.exists == 'true'
        run: npm run gate

      - name: Build
        if: steps.app.outputs.exists == 'true'
        env:
          # env.ts validates this at build time; a dummy value is fine for CI build.
          NEXT_PUBLIC_API_URL: http://localhost:8000
        run: npm run build

  # End-to-end browser tests (Playwright). Separate job so a flaky e2e never blocks
  # the fast verify signal, and so the browser download is cached independently.
  e2e:
    name: e2e (Playwright)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Detect app
        id: app
        run: |
          if [ -f package.json ]; then echo "exists=true" >> "$GITHUB_OUTPUT";
          else echo "exists=false" >> "$GITHUB_OUTPUT"; echo "::notice::No app yet — skipping e2e."; fi
      - uses: actions/setup-node@v4
        if: steps.app.outputs.exists == 'true'
        with:
          node-version: 'lts/*'
          cache: npm
      - name: Install
        if: steps.app.outputs.exists == 'true'
        run: npm ci
      - name: Install Playwright browsers
        if: steps.app.outputs.exists == 'true'
        run: npx playwright install --with-deps chromium
      - name: Run e2e
        if: steps.app.outputs.exists == 'true'
        env:
          NEXT_PUBLIC_API_URL: http://localhost:8000
        run: npm run test:e2e
      - name: Design-token conformance (AC-6)
        # Deterministic: fails on rendered color/spacing/radius/font values not in
        # DESIGN.md. Skips cleanly until the rigel-tokens block is filled in.
        if: steps.app.outputs.exists == 'true'
        env:
          NEXT_PUBLIC_API_URL: http://localhost:8000
        run: npm run test:design

  # Dependency vulnerabilities + leaked secrets. Runs on the bare harness too
  # (gitleaks scans history; audit no-ops without a lockfile).
  security:
    name: audit · secret-scan
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0 # gitleaks needs full history

      - name: Detect app
        id: app
        run: |
          if [ -f package-lock.json ]; then echo "lock=true" >> "$GITHUB_OUTPUT";
          else echo "lock=false" >> "$GITHUB_OUTPUT"; echo "::notice::No lockfile yet — skipping npm audit."; fi

      - uses: actions/setup-node@v4
        if: steps.app.outputs.lock == 'true'
        with:
          node-version: 'lts/*'
          cache: npm

      - name: npm audit (high+)
        if: steps.app.outputs.lock == 'true'
        run: npm audit --audit-level=high

      # Use the gitleaks CLI via Docker, NOT gitleaks-action — the action requires a
      # paid license on GitHub Organizations; the CLI/image is free everywhere.
      - name: Secret scan (gitleaks)
        run: docker run --rm -v "${{ github.workspace }}:/repo" ghcr.io/gitleaks/gitleaks:latest detect --source=/repo --redact --verbose

  # GitHub's dependency-review blocks PRs that introduce vulnerable/incompatible deps.
  # PR-only (the action requires a base..head diff).
  dependency-review:
    name: dependency-review
    if: github.event_name == 'pull_request'
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: write
    steps:
      - uses: actions/checkout@v4
      - uses: actions/dependency-review-action@v4
        with:
          fail-on-severity: high
          comment-summary-in-pr: on-failure
