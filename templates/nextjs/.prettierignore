.next
out
node_modules
dist
coverage
playwright-report
test-results
package-lock.json
# Auto-generated by Next on every build — not prettier-clean, gitignored. Excluding it
# keeps `format:check` (gate + CI) green after a build regenerates it.
next-env.d.ts
# Generated — never format (READ ONLY, regenerated by /api-sync)
src/types/api.generated.ts
# Machine-read policy + eval artifacts. Prettier expands the single-line objects in
# git-policy.json to multi-line, which breaks the toolchain-free single-line grep/sed
# readers in the .githooks + protect-branch.sh/check-protection-drift.sh. The format is a
# load-bearing contract, not prose to reformat. (PLAN-006 DF-15)
.rigel/
# Agent-managed plan/spec markdown. /build-layer appends Progress-Log entries and `ac:vector`
# appends an AC-vector block to the active plan .md — machine-appended content, not prose to
# reformat. `format:check` here is `prettier --check .` (whole tree, gate + CI), which would
# otherwise flag those appended blocks. Excluding keeps the gate green. (DF-45)
docs/exec-plans/
# Rigel-MANAGED paths (see `ownership` in .rigel/manifest.json). `verify:rigel` re-hashes
# every file here against the manifest and FAILS the gate on any byte change — so a
# `prettier --write .` (Step 8b of /infra-setup, and `npm run format`) that reformatted them
# would make the gate permanently red on a fresh scaffold. These files are Rigel's to update,
# not this repo's to reformat. (PLAN-008 AC-2)
.claude/
.github/
.githooks/
scripts/
knowledge/
