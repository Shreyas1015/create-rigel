// src/types/domain.types.ts
// Frontend-only types (UI state, form inputs, component props) that do NOT belong to the
// API contract. NEVER duplicate a shape that exists in api.generated.ts — import that
// instead. Empty until a feature needs a frontend-only type.
export {}
