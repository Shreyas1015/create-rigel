# ADR-000 — Acme's standard backend stack

**Status:** ACCEPTED (company-wide)

Every Acme service starts from the Rigel express template plus the `acme` layer. This file is
**seed**: it lands once, and your team owns it from then on — extend it with service-specific
decisions as ADR-001, ADR-002, …
